c_binary {
    name: "hello",
    srcs: [
        "hello.c",
    ],
    deps: ["extra"],
    cflags: [
        "-O3",
        "-Wall",
        "-Werror",
    ],
    ldflags: ["-Wall"],
}

run_script {
    name: "pointless_copy",
    script: "//build/simplebp/copy_out.py",
    inputs: [
        "hello.c",
    ],
    output: "{{.Input.Basename}}.out",
    args: "{{.Input.Name}} -o {{.Output}}",

}

subdirs = ["lib"]
